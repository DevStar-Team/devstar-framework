server:
  port: 8080

spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: traffic-web
      headers:
        application-name: traffic-web
        application-password: "kyochon"